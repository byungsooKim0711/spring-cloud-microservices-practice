server:
  port: 8888

spring:
##  profiles:
##    active: prod
  cloud:
    config:
      discovery:
        enabled: true

      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/byungsooKim0711/spring-cloud-config-server.git
          searchPaths: licensingservice, organizationservice